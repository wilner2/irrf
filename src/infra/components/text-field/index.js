import React from 'react';
import TextField from '@material-ui/core/TextField';

function MaterialTextField(props) {
  return <TextField {...props}>a</TextField>;
}

export default MaterialTextField;
